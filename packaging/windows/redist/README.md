Place `libcodec2.dll` here before building the Windows installer.

Build from https://github.com/drowe67/codec2 or obtain a pre-built Windows binary.
The DLL is not committed to git (see root `.gitignore`).
